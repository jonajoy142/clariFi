import { AppShell } from "@/features/shell/app-shell";
import { TaxReserveView } from "@/features/tax-reserve/tax-reserve-view";

export default function TaxReservePage() {
  return (
    <AppShell>
      <TaxReserveView />
    </AppShell>
  );
}
